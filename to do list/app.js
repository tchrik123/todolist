const input = document.querySelector(".input");
const btn = document.querySelector(".addBtn");
const done = document.querySelector(".done");
const close = document.querySelector(".close");

console.log(input, btn, done, close);
